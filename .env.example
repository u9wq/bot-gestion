# Token du bot — Discord Developer Portal > Bot > Reset Token
# C'est la seule valeur qui ne se configure pas depuis une commande :
# c'est un secret, et le bot doit redémarrer pour s'y reconnecter.
# Tout le reste se règle dans Discord (+config, +activity, +presence,
# +prefix, +setcolor, +setfooter).
TOKEN=
